[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # LLM & app settings: set VITE_* in Netlify UI (see NETLIFY_ENV.md). Not stored in this file.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
